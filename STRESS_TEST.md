# 本地压力测试报告

测试对象：v1.3.L（`sms.cpp` 当前源码，编译出的 `sms.exe`）
测试日期：2026-09-25
测试性质：本地回环（loopback）压测，服务端与压测客户端跑在同一台 Windows 11 x64 机器上。

## 一、测试方法

每轮压测都在独立临时目录里启动一份全新的服务（`sms.exe --server --port <端口>`，
数据用 500 条种子记录），不碰仓库里的 `students.txt`，跑完即销毁。共 9 个阶段：

| 阶段 | 内容 |
| --- | --- |
| 1 | 8 个客户端持续混合负载 15 秒：40% 查列表、20% 按姓名搜索、20% 新增、10% 改分、10% 删除 |
| 2 | 120 个连接同时打入（瞬时并发突发） |
| 3 | 先开 5 个"连上但不发数据"的空闲连接，再计时一个正常请求 |
| 4 | 3 个只发请求头、不发 body 的半截请求 |
| 5 | 畸形请求：1.5 MB 超大请求头、body 不是 JSON、不支持的 HTTP 方法、不存在的路径 |
| 6 | 持续写负载中 `taskkill -9` 强杀进程，检查数据文件 |
| 7 | 重启服务，核对数据是否完整、能否继续写 |
| 8 | 重启后再来一轮 8 客户端混合负载 10 秒 |
| 9 | 核对进程线程数/内存占用，以及最终文件与接口条数是否一致 |

压测客户端每个请求都新建一条 TCP 连接并带 `Connection: close`，与服务端
"一次连接一个请求"的处理方式一致。响应码 404（删除不存在的学号）和
409（学号重复）是负载配方里的预期结果，不算错误。

## 二、总体结果

| 指标 | 默认 1 个工作线程 | `--threads 2` |
| --- | --- | --- |
| 持续负载吞吐（8 客户端 × 15 s） | **939.7 请求/秒** | **1175.4 请求/秒** |
| 持续负载延迟 p50 / p95 / p99 | 8.6 / 10.8 / 12.4 ms | 6.6 / 10.6 / 12.2 ms |
| 持续负载最大延迟 | 21.1 ms | 18.7 ms |
| 负载期间的错误 | **0** | **0** |
| 120 连接突发：成功 / 被重置 | 65 / 55 | 66 / 54 |
| 5 个空闲连接下正常请求的等待 | 25 s（客户端超时未返回） | 19.5 s |
| 强杀后数据文件 | 1300 行、0 坏行、无残留 `.tmp` | 同左 |
| 重启后接口条数 = 文件行数 | 1300 = 1300 | 1300 = 1300 |
| 进程线程数（启动时） | 5（= 1 worker + 主线程 + accept + Winsock 3） | 6（= 2 worker + 主线程 + accept + Winsock 3） |
| 进程内存占用 | 5.3 MB 起，5.7 MB 收 | 同左 |

重启后的第二轮负载（阶段 8）两种配置分别是 930.1 和 886.2 请求/秒，
延迟 p50 都在 9 ms 左右，同样零错误。

## 三、各阶段明细

**阶段 1/8 持续负载**：默认 1 个 worker 就能扛住约 940 请求/秒，
p99 延迟 12 ms 以内，全程零错误；`--threads 2` 吞吐提高约 25%
（1175 请求/秒），但延迟只快 2 ms 左右。对本项目"本机单人使用"的定位，
默认值完全够用，`--threads 2` 的收益不明显。

**阶段 2 瞬时并发突发**：120 个连接在同一毫秒级窗口内打入，
约 45% 的连接被服务端直接断开（客户端报 `ECONNRESET`），
其余正常返回 200，整批在 150 ms 内结束，之后服务立即恢复正常。
原因是排队上限 64（`kMaxPending`）加上正在处理的连接，
超出部分由 accept 线程主动 `closesocket`。这是**有意的背压设计**：
宁可快速拒绝也不无限排队。只有同时发起几十个以上请求才会碰到
（例如几十个标签页一起刷新）。

**阶段 3 空闲连接**：连着但不发数据的连接会占满一个 worker 整整 10 秒
（单连接收发超时 `kSockTimeoutMs`）。worker 只有 1~2 个，
所以 5 个空闲连接就能把正常请求拖到 20~50 秒。影响有上限、会自愈，
不会永久卡死，但这段时间里页面操作会明显"转圈"。

**阶段 4 半截请求**：只发请求头、声称有 500 字节 body 却不发的连接，
服务端等了 10 秒（实测 10.0 s 时收到 `400 Bad Request`）后关闭连接，
worker 随即释放，不影响后续请求。

**阶段 5 畸形请求**：1.5 MB 的超大请求头被直接断开（不报错、不死锁）；
body 不是 JSON 返回 400；不支持的 HTTP 方法返回 405；
不存在的路径返回 404。全部符合 README 里的接口约定。

**阶段 6/7 强杀与重启**：在持续写负载中 `taskkill -9` 后，
`students.txt` 保持 1300 行、1300 个唯一学号、0 个坏行、
没有残留的 `.tmp`，末行完整；重启后接口条数与文件行数一致（1300 = 1300），
并可以继续写入。这是"先写 `.tmp` 再 `MoveFileEx` 原子替换"落盘策略的效果：
任何时刻磁盘上要么是旧的完整文件，要么是新的完整文件。

## 四、结论

1. **数据完整性没有问题**：强杀进程后零损坏、零截断，重启后数据与文件一致。
2. **常规使用性能充裕**：本机单人使用时，页面刷新、增删改都是个位数毫秒级，
   默认 1 个工作线程足够；压到约 940 请求/秒仍零错误。
3. **两个已知特征，均属设计取舍，不是缺陷**：
   - 瞬时并发超过约 66 个连接时，多出的连接被快速断开（`ECONNRESET`）；
   - 连上不发数据的连接会占用 worker 10 秒，少量空闲连接即可让正常请求等 20~50 秒。
4. **如果要支持多人同时高强度使用**，需要调整的正是上面两处：
   调大排队上限、缩短空闲连接超时、增加 worker 数——这三项目前都是
   v1.3.L 明确收紧过的参数（排队 64、超时 10 秒、worker 默认 1 上限 2）。

> 说明：服务端与客户端同机回环，吞吐数字相对真实局域网环境偏乐观；
> 但相对比较（1 worker 与 2 worker 的差异）和全部正确性结论不受影响。

---

## 附：本机硬件配置与 v1.3fix 复现实测（2026-09-26）

### 一、测试机硬件配置

| 项目 | 配置 |
| --- | --- |
| CPU | 13th Gen Intel Core i9-13900HX（Raptor Lake-HX，24 核 32 线程，基频 2.4 GHz） |
| 内存 | 16 GB |
| 操作系统 | Windows 11 专业工作站版 64-bit（Build 26200） |
| 编译 | VS 2026 Community `cl /nologo /EHsc /std:c++17 /W4 /utf-8`（零警告） |
| 压测客户端 | Node.js v24.21.0，每请求新建 TCP 连接 + `Connection: close` |
| 被测对象 | v1.3fix（`sms.exe`，`--server --port 4599`，500 条种子数据，默认 1 worker） |

### 二、复现结果与上文对比

| 指标 | 官方报告（默认 1 worker） | 本机 v1.3fix 实测 | 结论 |
| --- | --- | --- | --- |
| 持续负载吞吐（8 客户端 × 15 s） | 939.7 req/s | 602.7 req/s | 均 0 错误 |
| 持续负载延迟 p50/p95/p99 | 8.6/10.8/12.4 ms | 12.0/19.2/25.1 ms | 同量级 |
| 持续负载错误 | 0 | **0** | ✅ |
| 120 连接突发：成功/被重置 | 65/55 | **65/55** | ✅ 完全一致 |
| 5 空闲连接下正常请求 | 25 s（客户端超时未返回） | 50.0 s 后返回 200 | ✅ 符合 N×10s 设计 |
| 3 个半截请求 | 400（各 10 s 后） | 400（10/20/30 s） | ✅ |
| 强杀后数据文件 | 1300 行、0 坏行、无残留 | 1075 行、0 坏行、无 .tmp、末行完整 | ✅ 原子落盘有效 |
| 重启后 接口条数 = 文件行数 | 1300 = 1300 | 1075 = 1075 → 1876 = 1876 | ✅ |
| 进程线程数 / 内存 | 5 线程 / 5.3~5.7 MB | 5 线程 / 4.9 MB | ✅ |

吞吐绝对值低于官方报告约 35%，系客户端实现与同机 CPU 争用差异（Node 客户端 8 路并发循环
与服务端争抢 32 个逻辑处理器）；跨实现的相对结论（0 错误、背压计数、资源占用）完全一致。

### 三、复现中发现的两个接口层特性（官方报告未覆盖）

1. **不支持 `Transfer-Encoding: chunked` 请求体**：客户端以 chunked 发送 body 时
   （Node `http.request` 不显式设 `Content-Length` 的默认行为），录入接口返回
   `400 请求体不是有效的 JSON`；修改接口按 key 宽松提取仍能工作。
   浏览器与 curl 对已知长度 body 一律发送 `Content-Length`，实际使用不受影响。
2. **body 分多个 TCP 段到达时不丢失**：服务端会读满 `Content-Length` 指定的字节数，
   头部与 body 分包到达可正确处理（裸 socket 二分包实验验证）。
